% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes.R
\name{custom}
\alias{custom}
\title{Install a custom-branded Quarto theme}
\usage{
custom(
  colour,
  logo = NULL,
  favicon = NULL,
  name = "custom",
  title = NULL,
  path = ".",
  overwrite = FALSE,
  toc = NULL,
  toc_depth = NULL,
  code_fold = NULL,
  code_tools = NULL,
  self_contained = NULL,
  number_sections = NULL,
  highlight_style = NULL,
  warning = NULL,
  message = NULL,
  fig_width = NULL,
  fig_height = NULL
)
}
\arguments{
\item{colour}{Primary brand colour as a hex string (e.g. \code{"#003865"} or
\code{"#abc"}). Lighter and paler tints, and a readable header text colour,
are derived automatically.}

\item{logo}{Path to a PNG, SVG, or JPEG logo file. If \code{NULL}, the header
bar is rendered without a logo.}

\item{favicon}{Optional path to a dedicated favicon image. If \code{NULL}, the
logo is used as the favicon (and there is none when no logo is supplied).}

\item{name}{Theme name. Used (after sanitising to lowercase letters,
digits, and hyphens) as the extension folder name and the format key, so
\code{name = "My Lab"} installs \verb{_extensions/my-lab/} and is used as
\code{format: my-lab-html}. Defaults to \code{"custom"}.}

\item{title}{Human-readable title recorded in the extension's
\verb{_extension.yml}. Defaults to \code{"<name> HTML Format"}.}

\item{path}{Project root directory. Defaults to the current working
directory. Should be the top-level folder of your project so that all
\code{.qmd} files in subfolders can resolve the extension.}

\item{overwrite}{If \code{TRUE}, replace an existing \verb{_extensions/lshtm/}
directory. Defaults to \code{FALSE}.}

\item{toc}{Show a table of contents (\code{toc}). Logical, or \code{NULL} to leave
unset.}

\item{toc_depth}{Deepest heading level shown in the table of contents
(\code{toc-depth}, e.g. \code{3}). Requires \code{toc = TRUE} to have any effect.}

\item{code_fold}{Fold code blocks behind a toggle (\code{code-fold}). Logical.}

\item{code_tools}{Show the document-level code menu (\code{code-tools}). Logical.}

\item{self_contained}{Produce a single self-contained HTML file with all
assets inlined (sets both \code{embed-resources} and \code{self-contained}).
Logical.}

\item{number_sections}{Number section headings (\code{number-sections}). Logical.}

\item{highlight_style}{Syntax-highlighting theme for code
(\code{highlight-style}, e.g. \code{"github"}). Single string.}

\item{warning}{Show R warnings in the rendered output (\code{execute: warning}).
Logical; set \code{FALSE} to suppress.}

\item{message}{Show R messages in the rendered output (\code{execute: message}).
Logical; set \code{FALSE} to suppress.}

\item{fig_width}{Default figure width in inches (\code{execute: fig-width}).
Numeric.}

\item{fig_height}{Default figure height in inches (\code{execute: fig-height}).
Numeric.}
}
\value{
Invisibly returns the path to the installed extension directory.
}
\description{
The generic counterpart to the built-in department themes (\code{\link[=lshtm]{lshtm()}},
\code{\link[=lshtm_mrcg]{lshtm_mrcg()}}, \code{\link[=lshtm_mrcu]{lshtm_mrcu()}}, \code{\link[=florey]{florey()}}). Supply your own logo and a
single base colour and the package works out the rest:
}
\details{
\itemize{
\item lighter and paler tints of \code{colour} are derived automatically and used
for sidebar backgrounds, heading accents, and borders;
\item header text is set to white or near-black depending on whether \code{colour}
is perceptually dark (WCAG relative luminance), so the header bar stays
readable whatever colour you pick;
\item the logo is embedded as a base64 data URI so it renders correctly from
\code{.qmd} files anywhere in the project tree, and doubles as the browser-tab
favicon.
}

The extension is installed to \verb{_extensions/<name>/} under \code{path}, and a
\verb{_quarto.yml} is created at \code{path} if one does not already exist. Use
\verb{format: <name>-html} in your \code{.qmd} YAML.
}
\section{Optional format settings}{
The theme imposes no document defaults of its own. The format arguments
below are all unset (\code{NULL}) by default and are written into the extension
\emph{only} when you pass a value; anything left unset is omitted so Quarto's
own default applies. You can also set any of them per-document in your
\code{.qmd} YAML.
}

\examples{
\dontrun{
# Your organisation's logo and brand colour — the package derives the rest
usedepartmenttheme::custom(colour = "#003865", logo = "images/logo.png")

# A named theme, opting in to a table of contents and self-contained output
usedepartmenttheme::custom(
  colour         = "#6E2585",
  logo           = "images/my-lab.svg",
  name           = "My Lab",
  toc            = TRUE,
  self_contained = TRUE
)

# No logo: the header bar simply omits the image
usedepartmenttheme::custom(colour = "#B45C1F")
}
}
